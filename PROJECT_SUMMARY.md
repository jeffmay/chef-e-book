# Recipe Book — Project Changelog Summary

- [x] Monorepo scaffold (npm workspaces: shared, web, server)
- [x] Yjs data models in `shared` (Kitchenware, Recipe, Session, RecipeGroup, Measurement types with kind discriminators)
- [x] Measurement fraction arithmetic (simplify, add, subtract, multiply, divide) with exact rational representation
- [x] Unit conversion tables (US customary volume/weight and metric, exact within each system)
- [x] Default kitchenware CSV static asset (`packages/web/public/kitchenware.csv`) with 11 ingredients, 5 containers, 3 equipment; IDs left-padded to 12 chars with `"-"`; fetched and loaded by `useIngredientStore` on first page load if IndexedDB is empty
- [x] Vite + React 19 web app scaffold with e-ink CSS design system
- [x] Node.js + Express 5 sync server with Yjs document store endpoint
- [x] User selection (first-load page, localStorage persistence, per-user Yjs doc via y-indexeddb)
- [x] Top nav bar: hamburger NavMenu, Undo button, UserMenu with profile settings link
- [x] ProfileSettingsPage (rename user, persists to localStorage)
- [x] Yjs ingredient CRUD store (`shared`) — init from defaults, add/remove labels, set type/parent, rename, set labels
- [x] `useIngredientStore` hook + DocContext (React)
- [x] Ingredients Page — PrimeReact TreeTable tree view (expandable parent→child), in-table per-column filter row (text name filter with auto-expand; multi-select checkbox dropdown for type/labels), sortable columns, inline editable cells (click to edit, Enter/Escape hotkeys, ✔︎/✗ confirm/cancel), PrimeReact checkbox selection with bulk action bar (add/remove labels, change type, change parent), `+ New ingredient` form
- [x] `ItemLabel` type with branded `ItemLabelId` (7-char nanoid); `ItemKind`-scoped labels stored in `"labels"` Yjs map
- [x] Branded IDs for all item types via `ts-brand` + TypeScript declaration merging (`IngredientId`, `ContainerId`, `EquipmentId`, `ItemLabelId`)
- [x] `label_store.ts` — Yjs CRUD (add, find, rename, delete, find-or-create); `useLabelStore` React hook with cascade-delete observer and transactional merge
- [x] `LabelTable` expandable panel — multi-select checkboxes, bulk actions (Filter All, Filter Any, Delete, Merge with name prompt), inline rename; shown above `IngredientsTable` on the Bulk Ingredient Editor page
- [x] External label filter: selecting labels in `LabelTable` and clicking Filter All/Any updates a filter passed to `IngredientsTable` that pre-filters ingredients before tree-building
- [x] ArkType v2 schemas replace manual validation in `ingredient_store.ts` and `label_store.ts`; `MeasurementType` exported as both ArkType schema value and TypeScript type alias from `measurement.ts`
- [x] PapaParse replaces hand-written CSV parser in `parse_kitchenware_csv.ts`; per-kind ArkType schemas (`IngredientRow`, `ContainerRow`, `EquipmentRow`) with pipe morphs for "+" delimited label fields
- [x] `LabelEditor` component (`react-select` `CreatableSelect isMulti`) — add/remove existing labels from a dropdown or type to create a new one; replaces the plain text-input in the `IngredientsTable` Labels cell editor; cancel via ✗ button or Escape (when dropdown is closed)
- [x] `LabelEditor` bug fixes — `menuPlacement="auto"` prevents bottom cutoff; custom `LabelEditorMenu` component intercepts non-left-click mousedown to prevent right-click closing the dropdown; hover color updated to `#ebebeb`, active to `#c4c4c4`; bulk add/remove label actions in `IngredientsTable` now use `LabelEditor` instead of plain text inputs
- [x] `FractionEditor` component — inline fraction display (`integer<sup>num</sup>⁄<sub>denom</sub>` with aria-label); ± opens editor (replaced by <); < resets to pre-edit value (keeps editor open); ÷/×/−/+ radio buttons switch operation rows; op buttons apply math (simplified after each); `extraControls` slot between op buttons and OK; 20 component tests
- [x] `MeasurementEditor` component — same editing UX as FractionEditor sharing `FractionDisplay`, `OP_ROWS`, `OP_MODES`; adds type selector (volume/weight/count) and unit selector with US/metric optgroups; unit change converts fraction within same system (cross-system keeps value unchanged); OK converts to largest whole-number unit before committing; 20 component tests
- [x] `RecipeFolder` type — recursive ArkType scope; replaces flat `RecipeGroup`; `recipe_folder_store.ts` stores flat, builds tree via `buildFolderTree()`; backward-compat aliases exported
- [x] `Recipe` type overhaul — `RecipeId`, `RecipeVersionId`, `RecipeIngredientId` branded IDs; `RecipeIngredient` (id + ingredient_id + optional amount); `RecipeVersion` with `ingredients[]`, `sections[]`, `created_by`; `Recipe` with `title`, `subtitle?`, `source_url?`, `parent_folder_id?`; explicit TypeScript interfaces ensure branded-ID types survive cross-package inference
- [x] `recipe_store.ts` Yjs store — CRUD for recipes with structural validators; `createRecipe`, `saveRecipe` (in-place or new version), `copyRecipe`, `deleteRecipe`; all optional fields use conditional spread for `exactOptionalPropertyTypes` compatibility
- [x] `DurationEditor` component — text input with humanized display (`humanize-duration`), min/sec unit toggle, ±delta buttons (−5/−1/+1/+5 min or −15/−5/+5/+15 sec), revert `<` and commit `OK`; parse input via `parse-duration`; 24 component tests
- [x] `RecipeEditorPage` — full recipe CRUD UI; editor with title/subtitle/source URL/folder/version-description fields; `SectionEditor` (recursive, depth 1–5) with `IngredientSelector`-gated add for ingredients (both ingredient and measurement required), add/remove/edit for instruction/text-block/container/sub-section items; computed read-only ingredient totals derived from sections; version history table; `CopyRecipeDialog`; `DurationEditor` per instruction; `MeasurementEditor` per ingredient; description and amount validation with save guard; 33+ component tests. **Note: per-item notes (attach notes to sections/ingredients/instructions) are temporarily removed and will be re-implemented in a future branch.**
- [x] `useRecipeStore` and `useRecipeFolderStore` React hooks — Yjs-reactive stores for recipes and folders
- [x] ESLint config updated — `varsIgnorePattern: "^_"`, `ignoreRestSiblings: true` for destructure-discard patterns; removed `rules-of-hooks` override for hooks/contexts (now that hooks are camelCase)
- [x] All snake_case function names renamed to camelCase — `randomId`, `loadId`, `addFractions`, `getIngredients`, `useIngredientStore`, `buildFolderTree`, etc.; callback props renamed to camelCase (`onChange`, `onSave`, `onCancel`); file names remain snake_case
- [x] Naming convention rule formalized: camelCase for all component props, local variables, and constants; snake_case for Yjs document fields, Yjs map keys, file names, CSS classes, and string discriminator values (e.g. `"measurement_type"`, `"volume"`)
- [x] `IngredientsTable` migrated from `@tanstack/react-table` to PrimeReact `TreeTable` — removed grouping (no built-in equivalent), standalone `MultiSelectFilter` (no longer coupled to TanStack's `Column` type), filter bar above table instead of column-header filters, PrimeReact lara-light-indigo theme with CSS variable overrides to match the e-ink design system
- [x] `IngredientSelector` component — wraps PrimeReact `TreeSelect` for hierarchical ingredient selection; used in `IngredientsTable` inline parent cell editor and bulk parent action; mock-friendly via `vi.mock("primereact/treeselect")` in tests
- [x] `IngredientsTable` parent column uses `IngredientSelector` instead of plain `<select>` — inline cell editor and bulk action both use tree-select; bulk action bar adds a "Clear parent" button (sets parent to `undefined` without needing to select anything)
- [x] `LabelTable` filter buttons replaced with PrimeReact `RadioButton` group — "Filter: [All] [Any]" styled as button-group with black background / white text on selection; radio circle hidden with CSS clip trick for pure-CSS button appearance; selected mode persists in state
- [x] `Ingredient.default_measurement_type` → `default_measurement_value: Measurement` — backwards migration in `validateStored` auto-converts old Yjs entries; `DEFAULT_MEASUREMENT_BY_TYPE` constant maps type names to default Measurement values; `setMeasurementTypeForIngredients` → `setMeasurementValueForIngredients`
- [x] `Container.parent_id?` added — supports container hierarchy; `container_store.ts` Yjs CRUD store (`getContainers`, `addContainer`, `renameContainer`, `setLabelsForContainer`, `setParentForContainer`); `useContainerStore` React hook
- [x] `KitchenwareParentSelector` component — PrimeReact `TreeSelect` over container hierarchy; builds tree inline from `parent_id` links; CSS prefix `kps-`
- [x] `KitchenwareEditor` component — read-only name + locked kind="container"; `LabelEditor` for label management; `KitchenwareParentSelector` for parent; separate `onChangeLabels` / `onChangeParent` callbacks; CSS prefix `ke-`
- [x] `KitchenwareSelector` component — `react-select` `CreatableSelect` (single); opening/typing creates container immediately and shows modal with `KitchenwareEditor`; Create/Cancel actions; CSS prefix `ks-`
- [x] `RecipeFolderSelector` component — PrimeReact `TreeSelect` over folder tree; inline "+ Folder" button adds subfolder under selected folder (or root); Enter/Escape hotkeys; shows selected path as "/" separated breadcrumb; CSS prefix `rfs-`
- [x] `MeasurementEditor` refactor — new `onCancel` and `initially_open` props; `↩` button moved from header to bottom row (left of ✔︎) and now calls `revertAndClose()` which reverts value and calls `onCancel`
- [x] `IngredientsTable` "Type" column → "Default" — uses `MeasurementEditor` with `initially_open` for inline cell editing; `formatMeasurement` helper; bulk bar uses `MeasurementEditor` instead of type `<select>`; props renamed `onSetMeasurementValue` / `onBulkSetMeasurementValue`
- [x] `RecipeEditorPage` "+ amount" button opens `MeasurementEditor` in editing mode (via `initially_open`) with cancel support; container selector uses `KitchenwareSelector` (WIP — `COMMON_CONTAINERS` fallback replaced); folder selector wired to `RecipeFolderSelector`
- [x] `BulkIngredientEditorPage` add-form updated — `MeasurementEditor` replaces measurement type `<select>`; props updated for renamed ingredient store API
- [x] `IngredientsTable` standalone filter bar replaced by TreeTable's native per-column filter row — name/type/labels filters render as column `filterElement`s in `<th>` cells aligned with their columns; controlled `filters` prop with `filterMode="lenient"` keeps matching descendants' parents visible; custom `filterFunction`s handle the derived measurement-type and array-valued labels columns; table wrapper set to `overflow: visible` so the `MultiSelectFilter` dropdown is not clipped
- [x] `IngredientSelector` (`TreeSelect`) panel restyle — properly sized search/close/toggler SVG icons (PrimeReact `.p-icon` defaults to `1rem` and squishes in flex containers), repositioned magnifying-glass icon, indented child tree nodes; `MultiSelectFilter` option checkboxes no longer stretched by the global `input { width: 100% }` rule
- [x] React Router v7 Framework mode — `@react-router/dev/vite` plugin replaces `@vitejs/plugin-react` in Vite config; `root.tsx` is the new HTML shell + auth gate + nav layout (replaces `App.tsx`); `routes.ts` defines all routes explicitly; route components in `src/routes/` delegate to page components via `useOutletContext<RootContext>()`; `RecipeEditor` exported from `RecipeEditorPage` for direct use by `/recipes/:id` and `/recipes/:id/v/:vid` deep-link routes; `NavMenu` replaced button callbacks with `<Link>` components and `onBlur` blur-to-close; `UserMenu` also closes on blur
- [x] `RecipeEditor` folder field wired to `RecipeFolderSelector`; the selector's inline "+ Folder" button replaced by a **"New subfolder" checkbox** that reveals a required name input (shows the shared field error + disabled ✓ until named); on submit the editor creates the folder then sets the selector value to the new id
- [x] `InstructionIngredientSelector` component (`components/recipe_editor/`) — checkbox-mode PrimeReact `TreeSelect` populated from the recipe's own sections via `buildInstructionIngredientTree` (ingredients grouped under their containers; empty containers omitted; nested sections flattened); replaces `InstructionRow`'s flat store-wide ingredient checkbox list, so an instruction can only reference ingredients already added to a section/container; pure tree + selection-mapping helpers (`collectKeyToIngredientId`, `ingredientIdsToSelection`, `parseSelectionKeys`, `selectionToIngredientIds`); `COMMON_CONTAINERS` extracted to `components/recipe_editor/containers.ts`
- [x] Reusable form-field validation styles in `global.css` (`.field-required` `*` label indicator, `.field-input--error` heavy black outline, `.field-error` italic text with a `⚠` glyph via `::before`); applied to the editor's required Title and Version-description fields (each with a `*` indicator and `role="alert"` error); replaces the old red `re-field-*` error rules
- [x] Clicking the editor's "Create new version" checkbox now clears the version description and focuses its input, forcing a fresh version description
- [x] E-ink convention pass across pages: every page reserves bottom keyboard clearance (`--keyboard-clearance: 468px` + spacing on `.app > main` / `main.select-user-page`); text-input placeholders removed from the pages (descriptor, instruction action, text block, folder/merge names, book name, duration); muted text now resolves to full black and select empty-state labels render black; consolidated greyscale checkbox styling into `global.css` (native `input[type="checkbox"]` + PrimeReact `.p-checkbox-box`), fixing the `InstructionIngredientSelector` checkbox that stayed white-on-white because `.p-highlight` lands on the `.p-checkbox` wrapper
- [x] Recipe-editor section items: each `IngredientItemRow` now shows its own measurement value beside the name; a new section's header is hidden behind a `+ Add Section Header` button that reveals and focuses the input

---

## In Progress

_(none)_

---

## Completed

- [x] Document separation: kitchenware (ingredients, containers, labels) and recipes/folders are now separate Y.Doc instances (`useKitchenwareDoc` + `KitchenwareDocContext` and `useRecipeBookDoc` + `RecipeBookDocContext`)
- [x] Branded `UserId`, `RecipeBookId`, `RecipeBookName` types in shared package; `snakeCaseName` unbranded arktype (trims + normalizes to snake_case via `string-ts` `snakeCase`, brandable for different types)
- [x] Server storage layer: `DocumentStore` interface with three backends — `LocalMemoryStore` (in-memory Map, default for tests), `LocalFileStore` (filesystem, `./data/{bookId}.yjs`), `NetlifyBlobStore` (Netlify Blobs, store `"recipe-books"`, key `book-{bookId}`)
- [x] `POST /sync` updated: removed `:user_id` URL param; body is now `{ book_id: string, update?: string }`; loads/saves through `DocumentStore` interface
- [x] Server config via `arkenv`: `PORT`, `STORAGE_ENGINE` (`'local-memory' | 'local-file' | 'netlify-blobs'`), `NETLIFY_PROJECT_ID`
- [x] Fixed Vite version hoisting: added `vite@^8.0.10` to root `devDependencies` so `@react-router/dev` resolves vite 8 (which has `isRunnableDevEnvironment`) instead of vite 5 (from vitest's dependency tree)
- [x] `RecipeVersion` time fields — optional `estimated_time_seconds` (overrides the computed estimate) and `seconds_per_ingredient` (per-ingredient prep time); resolution chain: version value → RecipeBook setting → `VITE_DEFAULT_SECONDS_PER_INGREDIENT` build variable → 2-minute shared default (`DEFAULT_SECONDS_PER_INGREDIENT`)
- [x] Shared section helpers (`types/sections.ts`) — `collectIngredientItems`, `collectInstructions`, `computeTopIngredients` (moved from `RecipeEditorPage`, re-exported there), `removeSectionItemsById`
- [x] Estimation math (`math/estimation.ts`) — ingredient counting, instruction duration totals, minimum/resolved estimates, per-item progress weights (ingredients weigh the per-ingredient time; the remaining budget is split across instructions in proportion to their durations), `progressFraction`
- [x] `bookSettingsStore.ts` — `"book_settings"` Yjs map with optional `seconds_per_ingredient` (future settings page will edit it); `useBookSettings` hook resolves against the build default
- [x] `sessionStore.ts` — `"sessions"` Yjs map; `createSession`, `updateSessionItemState` (checked/skipped/notes merge), `completeSession` (records `completed_at`, auto-skips untouched items); completed sessions are never auto-deleted; `ItemState.skipped?` added; `Session.recipe_id`/`recipe_version_id` are now branded IDs
- [x] `RecipeSessionPage` (`/sessions/:session_id`) — run view renders the version's sections with checkboxes + Skip/Unskip per ingredient and instruction, containers as grouped ingredient lists, weighted progress bar with remaining-time text, and a Complete button; summary view shows total time, skipped items with per-item "Remove" checkboxes, an estimated-time slider clamped at the computed minimum, an adjustable time-per-ingredient `DurationEditor`, a required version description, and Create-a-new-version / Create-a-new-recipe / Discard actions
- [x] ▶ Start buttons — recipe editor header (starts the viewed version), version history table rows, and bulk recipe page recipe + version rows; all use `useStartSession` (creates a session, navigates to its page)
- [x] 🐛 `validateIngredientItem` (recipeStore) read/wrote the key `amount` while the `IngredientItem` model field is `customAmount`, silently dropping custom amounts on every load from Yjs; now reads `customAmount` (falling back to legacy `amount`) so amounts round-trip
- [x] 🐛 `RecipeEditor.handleSave` rebuilt the version object without the new time fields, so any editor save wiped `estimated_time_seconds`/`seconds_per_ingredient` set from a session summary; the editor now carries them over (it has no UI for them yet)
- [x] `ButtonMenu` component (`components/button_menu/`) — split button built from PrimeReact `ButtonGroup` + popup `Menu`: an optional `defaultButton` runs its action directly and a "▾" chevron lists all available actions; chevron-only when `defaultButton` is undefined; jsdom `offsetParent` shim added to `testSetup.ts` so PrimeReact overlay positioning works in tests
- [x] `RecipeVersionEditor` component (`components/recipe_editor/`) — extracted the computed Ingredients display + recursive Instructions section editor from `RecipeEditorPage` (helpers `computeTopIngredients`, `isSameMeasurementCategory`, etc. re-exported from the page for compatibility); optional `skippedIds`/`onRestoreItem`/`onDismissItem` render skipped rows with `--background-danger` stripes, a floating "skipped" tag, and Restore/✕ actions; `IngredientItemRow` now tolerates a not-yet-loaded kitchenware store instead of throwing
- [x] Session summary rework — replaced the skipped-item checkbox rows with the full `RecipeVersionEditor` (new ingredients/sections can be added before saving); Restore puts a skipped item back, ✕ removes the row, and still-skipped items are dropped from any saved version by default; the estimated-time slider minimum now tracks the kept items; a vertical rule separates the "Time per ingredient" label from its duration selector
- [x] `ButtonMenu` adoption — bulk recipe page recipe/version rows (▶ Start default, Start/Edit in menu), folder + virtual-root "New" menus (New Recipe default, New Recipe/New Folder in menu, replacing the hand-rolled `NewItemMenuDropdown`), and recipe editor version-history rows (Edit default, Start in menu)
- [x] Mobile view for the bulk recipe page — new `useMobileView` hook (`hooks/useMobileView.ts`, `MOBILE_VIEW_MAX_WIDTH` = 600px, reactive `matchMedia`) and a matching `@media (max-width: 600px)` rule; at ≤ 600px the Created/Updated columns collapse (leaving Select/Name/Actions) and every actions `ButtonMenu` gets `hideDefault` (default action folds into the "▾" menu). Added a `hideDefault` prop to `ButtonMenu` for this
- [x] Mobile focus-zoom prevention — a global `@media (max-width: 600px)` rule in `global.css` forces focusable text inputs/textarea/select to `font-size: 16px !important` so mobile browsers (notably iOS Safari) no longer auto-zoom the page on input focus; guarded by a `styles/__tests__/global.test.ts` invariant test
- [x] Depth-lines column for the bulk ingredient editor (`IngredientsTable`, all viewport sizes) — replaces the Name column's per-level horizontal indentation with a fixed sub-16px column (`.it-depth` / `.it-depth-line`, rendered by `DepthLines`, one vertical line per nesting level; PrimeReact toggler indentation zeroed). The lines are absolutely anchored to the name `<td>` so they fill the full row height regardless of text wrapping and sit to the left of the checkbox (via `padding-left` on the first body cell); roots reserve the column so checkboxes stay aligned
- [x] Mobile view for the bulk ingredient editor (`IngredientsTable`) — a `@media (max-width: 600px)` rule reclaims further horizontal space: table header text wraps, inline-edit accept/cancel buttons stack below their field, and the bulk edit fields stack vertically. The bulk-action bar groups "{count} selected" + a right-floated "Clear" into an `.it-bulk-header` block, and captions each action with a visible label ("Labels to add", "Labels to remove", "Set default measurement", "Set parent"). The two label editors are folded up by default — each expands via a solid-triangle toggle (`.it-bulk-fold-toggle`, "▼"/"▲") in its own button, rendering its label editor only while expanded and showing a pending-count badge while folded. "Set parent" is a single `IngredientSelector` + ↩/✔︎ editor matching the other IngredientTable editors (✔︎ applies the parent to all selected rows, or clears it when "— None —"). In mobile view the "Set default measurement" and "Set parent" actions (`.it-bulk-action--inline`) stay horizontal, and the `LabelEditor`'s selected-labels view + ↩/✔︎ buttons are a row from 1000px→400px and stack into a column below 400px via a `!important`-free specificity ladder (`.it-label-editor` qualifier at ≤1000px, doubled class at <400px)
- [x] Inline-edit rows with accept/cancel (`RecipeVersionEditor`) — instruction **and** container rows collapse to a one-line summary (`summarizeInstruction` / `summarizeContainer` in `components/recipe_editor/drafts.ts`) and open into an inline editor whose changes live in a local draft until "✔︎"; "↩" reverts, or removes the row when it was added in this editing session and never accepted (`isNew`, tracked per `SectionEditor`). Accepting merges field-wise against the snapshot taken when the editor opened, so untouched fields keep the live value and concurrent edits survive (`mergeInstructionDraft` / `mergeContainerDraft`). A container's nested ingredient rows stay visible while its header is collapsed and still write through immediately. `COMMON_EQUIPMENT` extracted to `components/recipe_editor/equipment.ts`
- [x] Unsaved-draft prompt on save (`RecipeEditor`) — rows holding uncommitted drafts register with a `PendingSectionEdits` registry (`components/recipe_editor/pendingEdits.ts`, provided by `RecipeVersionEditor` via the optional `pendingEdits` prop). Saving with any open draft shows `PendingEditsDialog` ("Accept all changes" / "Discard all changes" / "Cancel") instead of silently dropping the edits. Both resolutions are applied to the section tree in one pass (`applySectionItemUpdates` + `removeSectionItemsById` in `shared/types/sections.ts`) so simultaneous row commits cannot clobber each other
- [x] Narrow view for the bulk ingredient editor (`IngredientsTable`, < 450px) — a `@media (max-width: 449.98px)` rule hides the Default Value and Parent columns (each cell carries an `it-col--measurement` / `it-col--parent` class set via the columns' `headerClassName`/`bodyClassName`/`filterHeaderClassName`); those values are edited through the bulk-action bar instead, while Name and Labels stay inline-editable
- [x] Accept/cancel on the remaining `RecipeVersionEditor` rows — section headers and ingredient rows now confirm with ↩/✔︎ like the instruction and container rows. Both write through as they are edited (so a name change shows immediately) and "↩" restores the value the row held when its editor opened; a header added in that pass is dropped entirely. An ingredient cancel reverts only the fields its editor can change (`revertIngredientItem`), keeping the rest from the live item like the drafted rows' merges. A collapsed ingredient row floats an "✎" edit button to the left of its "−" remove button in the row's upper-right corner
- [x] Container header editor rework — the container type is a labelled "Type" select, the descriptor moved behind a "+ Name" button that reveals and focuses a labelled "Name" input, and a new shared `InfoTip` ("ⓘ", `components/info_tip/`) explains the "ordered" flag on press (no hover state on the target e-ink tablet), linking the revealed text to its toggle with `aria-controls`/`aria-describedby`. Type, name, and the ↩/✔︎ actions lay out in a row, stacking into a column below 600px. Both reveal-then-focus flows (the container name and the section header) focus from an effect in the commit that mounts the input rather than a `setTimeout`, so no timer can fire at a cancelled row
- [x] One row-control vocabulary across the `RecipeVersionEditor` — every removable row (ingredient, container, instruction, text block, section header) now uses the same "✎" edit / "✕" remove buttons, replacing the old "−" and "×" glyphs and the click-the-name/double-click-the-name edit handlers. "✎" leads the row (left of the text it edits) and "✕" floats in the upper-right corner, so a stylus cannot hit one while reaching for the other, and both render as square tap targets rather than bare glyphs. The controls are shared components: `EditActions` (`components/edit_actions/`, the "↩"/"✔︎" pair) plus `EditButton` and `RemoveButton` (`components/row_actions/`, each taking a required `label` + `onClick` so a button can never render unnamed). Text blocks joined the drafted collapse-and-reopen pattern (`mergeTextBlockDraft`), so the dashed `re-item--new` border no longer contradicts the tree: the empty row is added immediately, but none of its content reaches the tree until "✔︎". Rows open into their editor on `isNew` rather than on the item looking blank, so a row accepted while still empty collapses to its summary instead of re-opening as an editor on every mount (retiring `isBlankInstruction` / `isBlankContainer`). Rows reserve the floated "✕" on both axes (a `--row-action-size` `padding-right` and a matching `min-height`), so it can no longer spill onto the row below when the row is shorter than the button, and "✎" is `align-self: flex-start` so it stays level with "✕" on a multi-line row. `EditActions` follows the fields it commits inline and wraps to the next line left-aligned instead of floating right, which retired its `className` escape hatch and the `.re-item-editor-actions` overrides, and its buttons now match the row actions' `--row-action-size` height. The session summary owns a `usePendingSectionEdits()` registry too, so "Create a new version" / "Create a new recipe" prompt with `PendingEditsDialog` instead of dropping a row left open
- [x] Persisted tree state on the bulk recipe page — the tree now starts **fully expanded** (root, every folder, every recipe's versions) and each device remembers what it collapsed across page loads. New `useLocalViewState` hook (`hooks/useLocalViewState.ts`) is the general pattern for state that describes how _this device_ views the book rather than what is in it: it takes a `localStorage` key, an `arktype` `Companion` to validate whatever is stored, and a default factory; reads lazily on the first render (so a reload paints the restored view instead of flashing the default), warns and falls back to the default on a malformed or schema-stale value (reporting unparsable JSON separately from a schema mismatch), and runs each update inside `setState` — writing through to `localStorage` in the same updater — so consecutive updates in one tick compose without a ref written during render. All storage access goes through a new `storage/safeLocalStorage.ts` (`readLocalStorage` / `writeLocalStorage` / `removeLocalStorage`, also adopted by `useActiveBookMetaStore`), which guards both a missing global (build-time prerender) and a _throwing_ one (`SecurityError` in sandboxed/privacy-blocked contexts, `QuotaExceededError` on write) — the read happens during the first render, where an unguarded throw would take the page down on load. `BulkRecipeViewState` (`pages/bulkRecipeViewState.ts`, key `"chefe_bulk_recipe_view"`) deliberately stores the **collapsed** ids (`root_collapsed`, `collapsed_folder_ids`, `collapsed_recipe_ids`) rather than the expanded ones, so the empty state means "fully expanded" and a folder or recipe created after the state was last written starts expanded like everything else
- [x] Bolded summary heads — collapsed container rows render their type in bold ("**Bowl** — dry ingredients") and collapsed instruction rows their action ("**Bake** the Flour in the Oven"), via `containerDisplayName` + `containerSummaryDetails` / `instructionSummaryName` + `instructionSummaryDetails` in `components/recipe_editor/drafts.ts`. The rows render each half as its own element, so the combined `summarizeContainer` / `summarizeInstruction` composers moved into `drafts.test.ts`, where they assert the full one-line summaries over the two exported halves

---

## Future Development Goals

- Re-implement per-item notes (attach notes to sections, ingredients, instructions, text blocks) — temporarily removed; see `recipe-editor-ingredient-selector` branch
- Hardware display target: PineNote with a 1404 x 1872 pixel black & white (with dithering) e-ink screen
- Multi-user collaboration (shared Yjs documents)
- Large default list of ingredients and default measurement values
- AI-assisted recipe scraping
- Export to PDF / print view
